---
name: Wrong status expectation - should fail

request:
  url: /api/users

backends:
  default:
    status: 200
    body: '{"users": []}'

expectations:
  response:
    status: 404  # This is WRONG - we'll actually get 200
