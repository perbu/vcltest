---
name: Wrong status expectation - should fail
vcl: examples/error-demo.vcl

request:
  url: /api/users

backend:
  status: 200
  body: '{"users": []}'

expect:
  status: 404  # This is WRONG - we'll actually get 200
  backend_calls: 1
